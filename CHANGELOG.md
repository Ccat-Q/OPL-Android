# 更新日志

本文档根据 Git 提交记录整理。统计范围为 `1.0.3.5` 之后的首个版本提交 `52befb7`，截至当前提交 `57e221e`；共包含 22 个提交，其中 20 个为非合并提交。

## [1.0.5.2] - 2026-08-20

### 修复

- 适配 EasyTier 新版 CLI 节点列表格式，支持 ASCII 表格以及 `lat(ms)`、`rx`、`tx`、`tunnel`、`NAT` 等新列名。
- 保留对 EasyTier 旧版 Unicode 表格和旧列名的兼容。
- 修复节点输出被隔行丢弃、IPv4 CIDR 后缀处理错误的问题。

## [1.0.5.1] - 2026-08-20

### 界面与体验

- 隧道列表为空时显示操作引导。
- 服务状态增加文字说明，不再仅通过颜色表示连接状态。
- 优化隧道卡片列宽和操作区布局。
- 统一添加、编辑隧道窗口的表单尺寸和必填项提示。
- 支持 Enter 确认和 Esc 取消弹窗操作。

## [1.0.5.0] - 2026-08-20

### 新增

- 重构主界面为统一的导航、页面标题、卡片和底部操作栏布局，并统一各功能弹窗样式。
- 新增独立隧道列表控件，集中展示名称、UID、协议、端口、连接地址和状态。
- 隧道支持拖动排序，并保留启用、编辑、删除和复制本地地址操作。
- EasyTier 支持添加、删除和持久化多个公共节点；自动校验、去重并规范化 `tcp://`、`udp://` 节点地址。
- 新增 EasyTier 核心自动更新选项和完整性校验。
- 新增“退出时清理日志缓存”设置。
- 公告改为卡片列表展示，并增加获取失败提示。

### 改进

- 程序运行期间统一禁用会修改配置或与运行状态冲突的操作，并补充清晰的禁用样式。
- 更新信息从预设数据中拆分到独立的 `update.json`，应用、OpenP2P 与 EasyTier 核心可以分别更新。
- OpenP2P 更新包在解压后执行文件校验，失败时停止更新并记录错误。
- 配置文件为空、损坏或字段缺失时保留上一次有效配置，避免启动和刷新列表时崩溃。
- EasyTier 连续获取不到节点状态时提供日志反馈并安全退出网络。
- 清理旧组网代码、废弃控件和冗余实现。

### 构建与发布

- 使用新的 GitHub Actions 发布流程替换旧构建工作流。
- 发布流程可自动构建 OpenP2P 核心、生成应用包和 MD5、更新远程发布清单。
- 修正发布分支、打包路径和 JSON 输出，并为正式发布增加人工审批环境。

## [1.0.4.0] - 2026-07-02

### 改进

- 协议更新包号由 75 升级至 76。
- 移除不再使用的“一言”功能及相关界面和代码。
- 调整主窗口尺寸、UID 区域和整体布局。

### 修复

- 修复关闭窗口最小化到托盘后无法正确隐藏或恢复窗口的问题（#17）。

